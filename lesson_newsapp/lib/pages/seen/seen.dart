import 'package:flutter/material.dart';

class SeenPage extends StatelessWidget {
  const SeenPage({super.key});

  static const String routerName = '/seen_page';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
